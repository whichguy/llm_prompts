name: AgentWatch

on:
  issue_comment:
    types: [created, edited]
  pull_request_comment:
    types: [created, edited]
  workflow_dispatch:

jobs:
  agentwatch:
    uses: whichguy/agentwatch/.github/workflows/agentwatch.yml@main
    secrets: inherit
